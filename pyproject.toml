[tool.poetry]
name = "ls"
version = "0.1"
description = "BryuchanovMA"
authors = ["Bryuch"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
